// const items = document.querySelectorAll(".item");
// items.forEach((item) => {
//     console.log(item);
// })
const items = document.querySelectorAll('.item')
items.forEach((item) => {
    console.log(item.innerText)
})

// item.remove();
